apiVersion: v1
kind: Service
metadata:
  name: emunet-redis
  namespace: kube-system
  labels:
    app: emunet-redis
spec:
  type: ClusterIP
  selector:
    app: emunet-redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
